/* eslint-disable no-unused-vars */

/**
 * Initializes the carousel by updating DOM with out own classes.
 * @param {string} contactEmail - The input email address.
 * @return {string}
 */
function contact(contactEmail) {
  // TODO: Email to come. Need to set up DNS.
  const emailStr = `<strike><a href="">${contactEmail}</a></strike>`;
  return emailStr;
};
